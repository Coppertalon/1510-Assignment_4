from unittest import TestCase


class Test(TestCase):

    # the game_set_up runs in a user centered while loop and is therefore too difficult to test
    def test_game_set_up(self):
        self.fail()
